# Copy to infra/.env and adjust if you want different local credentials.
# docker-compose.yml already has working defaults, this file is only
# needed if you want to override them.
POSTGRES_DB=sentinel
POSTGRES_USER=sentinel
POSTGRES_PASSWORD=sentinel
